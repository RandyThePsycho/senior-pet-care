# Supabase 连接配置
# 在 Supabase 项目设置 > API 中获取。
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# 仅服务端使用！绝不可暴露给 client component / 浏览器。
# 用于 API route 内的写入与读取（绕过 RLS）。
SUPABASE_SERVICE_ROLE_KEY=
